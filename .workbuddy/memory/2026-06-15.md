# 2026-06-15 工作日志

## 修复 AuthServiceImpl.java 编译错误
- 问题：354 行 `JwtUtil.generateToken(user.getUserId())` 只传了 1 个参数，但方法签名要求 3 个 `(Long, Integer, Integer)`
- 修复：改为 `JwtUtil.generateToken(user.getUserId(), roleId, storeId)`，roleId/storeId 在上方 340-351 行已查好

## 修复 app.json JSON 语法错误
- 问题：第 23 行 `"pages/hq/promo-form/promo-form"` 末尾缺少逗号，导致小程序模拟器启动失败（Expecting EOF got STRING）
- 修复：添加逗号 → `"pages/hq/promo-form/promo-form",`
